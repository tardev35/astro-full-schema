---
export interface Props {
  title?: string;
  as?: "h1" | "h2" | "h3" | "h4" | "h5" | "h6";
}
const { title, as: HeadingTag = "h1" } = Astro.props;
---

<section class="relative z-10 my-8 flex w-full justify-center px-4 text-center font-sans">
  <div
    class="group relative w-full max-w-[1400px] overflow-hidden rounded-3xl border border-red-500/30 bg-[#5f0914]/60 p-8 text-white/90 shadow-[0_10px_30px_rgba(0,0,0,0.5)] backdrop-blur-xl transition-all duration-500 hover:border-yellow-400/40 hover:shadow-[0_15px_40px_rgba(250,204,21,0.15)] md:p-10">
    <div class="pointer-events-none absolute -top-20 -left-20 h-40 w-40 rounded-full bg-red-500/20 blur-[50px] transition-all duration-500 group-hover:bg-red-400/30"></div>
    <div class="pointer-events-none absolute -right-20 -bottom-20 h-40 w-40 rounded-full bg-yellow-500/10 blur-[50px] transition-all duration-500 group-hover:bg-yellow-400/20"></div>

    {
      title && (
        <header class="mb-6 flex items-center gap-4 border-b border-red-500/30 pb-4">
          <div class="h-8 w-1.5 rounded-full bg-gradient-to-b from-yellow-300 to-yellow-500 shadow-[0_0_10px_rgba(250,204,21,0.5)]" />
          <HeadingTag class="bg-gradient-to-r from-yellow-300 to-yellow-500 bg-clip-text text-2xl font-black tracking-widest text-transparent uppercase drop-shadow-sm">{title}</HeadingTag>
        </header>
      )
    }
    <div class="relative z-10 text-base leading-relaxed text-red-50/90">
      <slot />
    </div>
  </div>
</section>
<style>
  .font-sans {
    font-family: "Kanit", sans-serif;
  }
</style>
