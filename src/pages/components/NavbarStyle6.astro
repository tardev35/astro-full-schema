---
import { Picture } from "astro:assets";
const images = import.meta.glob<{ default: ImageMetadata }>("../../assets/logo.{png,webp,jpg}", { eager: true });

const imagePaths = Object.keys(images);
const mainLogo = imagePaths.length > 0 ? images[imagePaths[0]].default : null;

const currentPath = Astro.url.pathname;

const isActive = (path: string) => {
  if (path === "/") return currentPath === "/";
  return currentPath.startsWith(path);
};
---

<header class="sticky top-0 z-50 w-full font-sans">
  <div class="pointer-events-none absolute top-0 left-0 z-50 hidden h-0 w-full items-start justify-center lg:flex">
    <div
      class="pointer-events-auto flex h-[65px] w-[120px] items-center justify-center rounded-b-3xl border-x border-b border-yellow-500 bg-gradient-to-b from-[#1a1a2e] to-[#0a0a14] shadow-[0_5px_15px_rgba(234,179,8,0.3)]">
      <a href="/" class="drop-shadow-[0_0_8px_rgba(255,255,255,0.2)] transition-transform hover:scale-110">
        <Picture src={mainLogo!} alt="Gamer Notch Logo" loading="eager" fetchpriority="high" class="h-[35px] w-auto" formats={["avif", "webp"]} fallbackFormat="webp" />
      </a>
    </div>
  </div>

  <aside
    class="fixed top-1/2 left-3 z-40 hidden h-[450px] w-[90px] -translate-y-1/2 flex-col items-center justify-center rounded-2xl border border-white/10 bg-[#1a1a2e]/90 p-2 shadow-[5px_0_20px_rgba(0,0,0,0.5)] backdrop-blur-md lg:flex">
    <div class="relative flex h-full w-full flex-col gap-2 overflow-hidden rounded-xl">
      <a href="/" class={`group relative w-full h-full flex flex-col items-center justify-center rounded-lg transition-all ${isActive("/") ? "bg-[#380505]" : "hover:bg-[#380505]"}`}>
        <span class="mb-1 text-3xl drop-shadow-md transition-transform group-hover:scale-110">🏠</span>
        <span class={`text-xs font-bold transition-colors ${isActive("/") ? "text-yellow-400" : "text-gray-400 group-hover:text-white"}`}>หน้าแรก</span>
        <div class={`absolute left-0 top-0 h-full w-[3px] bg-yellow-500 transition-transform duration-300 origin-top ${isActive("/") ? "scale-y-100" : "scale-y-0 group-hover:scale-y-100"}`}></div>
      </a>

      <a href="/promotion" class={`group relative w-full h-full flex flex-col items-center justify-center rounded-lg transition-all ${isActive("/promotion") ? "bg-[#380505]" : "hover:bg-[#380505]"}`}>
        <span class="mb-1 text-3xl drop-shadow-md transition-transform group-hover:scale-110">🎁</span>
        <span class={`text-xs font-bold transition-colors ${isActive("/promotion") ? "text-yellow-400" : "text-gray-400 group-hover:text-white"}`}>โปรโมชั่น</span>
        <div class={`absolute left-0 top-0 h-full w-[3px] bg-yellow-500 transition-transform duration-300 origin-top ${isActive("/promotion") ? "scale-y-100" : "scale-y-0 group-hover:scale-y-100"}`}>
        </div>
      </a>

      <a href="/gamedemo" class={`group relative w-full h-full flex flex-col items-center justify-center rounded-lg transition-all ${isActive("/gamedemo") ? "bg-[#380505]" : "hover:bg-[#380505]"}`}>
        <span class="mb-1 text-3xl drop-shadow-md transition-transform group-hover:scale-110">🎰</span>
        <span class={`text-xs font-bold transition-colors ${isActive("/gamedemo") ? "text-yellow-400" : "text-gray-400 group-hover:text-white"}`}>ทดลองเล่น</span>
        <div class={`absolute left-0 top-0 h-full w-[3px] bg-yellow-500 transition-transform duration-300 origin-top ${isActive("/gamedemo") ? "scale-y-100" : "scale-y-0 group-hover:scale-y-100"}`}>
        </div>
      </a>

      <a href="/gamelogin" class={`group relative w-full h-full flex flex-col items-center justify-center rounded-lg transition-all ${isActive("/gamelogin") ? "bg-[#380505]" : "hover:bg-[#380505]"}`}>
        <span class="mb-1 text-3xl drop-shadow-md transition-transform group-hover:scale-110">🌐</span>
        <span class={`text-xs font-bold transition-colors ${isActive("/gamelogin") ? "text-yellow-400" : "text-gray-400 group-hover:text-white"}`}>ทางเข้า</span>
        <div class={`absolute left-0 top-0 h-full w-[3px] bg-yellow-500 transition-transform duration-300 origin-top ${isActive("/gamelogin") ? "scale-y-100" : "scale-y-0 group-hover:scale-y-100"}`}>
        </div>
      </a>

      <a href="/contact" class={`group relative w-full h-full flex flex-col items-center justify-center rounded-lg transition-all ${isActive("/contact") ? "bg-[#380505]" : "hover:bg-[#380505]"}`}>
        <span class="mb-1 text-3xl drop-shadow-md transition-transform group-hover:scale-110">📞</span>
        <span class={`text-xs font-bold transition-colors ${isActive("/contact") ? "text-yellow-400" : "text-gray-400 group-hover:text-white"}`}>ติดต่อเรา</span>
        <div class={`absolute left-0 top-0 h-full w-[3px] bg-yellow-500 transition-transform duration-300 origin-top ${isActive("/contact") ? "scale-y-100" : "scale-y-0 group-hover:scale-y-100"}`}>
        </div>
      </a>
    </div>
  </aside>

  <nav class="relative z-40 flex h-[60px] items-center justify-between border-b border-red-900/50 bg-[#1a1a2e] px-3 shadow-2xl lg:hidden">
    <button id="hamburger-btn" class="flex flex-col items-center justify-center space-y-1.5 p-2" aria-label="Menu">
      <span class="hamburger-line h-0.5 w-6 origin-center bg-white transition-all duration-300"></span>
      <span class="hamburger-line h-0.5 w-6 bg-white transition-all duration-300"></span>
      <span class="hamburger-line h-0.5 w-6 origin-center bg-white transition-all duration-300"></span>
    </button>

    <a href="/" class="flex-shrink-0 transition-transform hover:scale-105">
      <Picture src={mainLogo} alt="Main Logo Mobile" loading="eager" fetchpriority="high" class="h-[30px] w-auto drop-shadow-lg" formats={["avif", "webp"]} fallbackFormat="webp" />
    </a>

    <a
      href="/signin"
      class="flex h-[32px] items-center justify-center rounded border border-red-500/20 bg-gradient-to-b from-red-600 to-red-700 px-4 text-xs font-bold text-white shadow shadow-black/50 hover:brightness-110">
      เข้าสู่ระบบ
    </a>
  </nav>

  <div id="mobile-overlay" class="pointer-events-none fixed inset-0 z-30 bg-black/80 opacity-0 backdrop-blur-sm transition-opacity duration-300 lg:hidden"></div>

  <div
    id="mobile-sidebar"
    class="fixed top-0 left-0 z-50 h-screen w-[260px] -translate-x-full transform overflow-y-auto border-r border-red-900/50 bg-[#1a1a2e] shadow-[10px_0_30px_rgba(0,0,0,0.8)] transition-transform duration-300 ease-out lg:hidden">
    <div class="flex flex-col space-y-2.5 px-4 pt-16 pb-8">
      <a
        href="/"
        class={`sidebar-item flex items-center px-4 py-3 rounded-lg border transition-all ${isActive("/") ? "bg-red-900/30 border-red-700 text-yellow-400" : "bg-white/5 border-transparent text-gray-300 hover:bg-white/10 hover:text-white"}`}>
        <span class="mr-3 text-xl">🏠</span>
        <span class="text-sm font-bold">หน้าแรก</span>
      </a>
      <a
        href="/promotion"
        class={`sidebar-item flex items-center px-4 py-3 rounded-lg border transition-all ${isActive("/promotion") ? "bg-red-900/30 border-red-700 text-yellow-400" : "bg-white/5 border-transparent text-gray-300 hover:bg-white/10 hover:text-white"}`}>
        <span class="mr-3 text-xl">🎁</span>
        <span class="text-sm font-bold">โปรโมชั่น</span>
      </a>
      <a
        href="/gamedemo"
        class={`sidebar-item flex items-center px-4 py-3 rounded-lg border transition-all ${isActive("/gamedemo") ? "bg-red-900/30 border-red-700 text-yellow-400" : "bg-white/5 border-transparent text-gray-300 hover:bg-white/10 hover:text-white"}`}>
        <span class="mr-3 text-xl">🎰</span>
        <span class="text-sm font-bold">ทดลองเล่น</span>
      </a>
      <a
        href="/gamelogin"
        class={`sidebar-item flex items-center px-4 py-3 rounded-lg border transition-all ${isActive("/gamelogin") ? "bg-red-900/30 border-red-700 text-yellow-400" : "bg-white/5 border-transparent text-gray-300 hover:bg-white/10 hover:text-white"}`}>
        <span class="mr-3 text-xl">🌐</span>
        <span class="text-sm font-bold">ทางเข้า</span>
      </a>
      <a
        href="/contact"
        class={`sidebar-item flex items-center px-4 py-3 rounded-lg border transition-all ${isActive("/contact") ? "bg-red-900/30 border-red-700 text-yellow-400" : "bg-white/5 border-transparent text-gray-300 hover:bg-white/10 hover:text-white"}`}>
        <span class="mr-3 text-xl">📞</span>
        <span class="text-sm font-bold">ติดต่อเรา</span>
      </a>

      <div class="mt-5 flex flex-col gap-2 border-t border-red-900/50 pt-5">
        <a href="/signup" class="sidebar-item flex justify-center rounded border border-red-500/20 bg-gradient-to-r from-red-600 to-red-700 py-3 text-sm font-bold text-white shadow shadow-black/50">
          🔥 สมัครสมาชิกใหม่
        </a>
      </div>
    </div>
  </div>

  <div class="fixed top-4 right-6 z-50 hidden items-center gap-3 lg:flex">
    <a href="/signin" class="group flex items-center gap-1.5 rounded border border-white/5 bg-[#0a0a14] px-6 py-2 text-sm font-bold text-white/70 shadow-md transition-all hover:text-white">
      🔒 เข้าสู่ระบบ
    </a>
    <a
      href="/signup"
      class="group flex items-center gap-1.5 rounded border border-red-500/20 bg-gradient-to-r from-red-600 to-red-700 px-6 py-2 text-sm font-bold text-white shadow-[0_0_15px_rgba(220,38,38,0.5)] transition-all hover:scale-105 hover:brightness-110">
      👤 สมัครสมาชิก
    </a>
  </div>
</header>

<div class="hidden h-[15px] w-full lg:block"></div>

<style is:global>
  /* Hamburger กลายเป็น X */
  #hamburger-btn.active .hamburger-line:nth-child(1) {
    transform: translateY(8px) rotate(45deg);
  }
  #hamburger-btn.active .hamburger-line:nth-child(2) {
    opacity: 0;
    transform: scaleX(0);
  }
  #hamburger-btn.active .hamburger-line:nth-child(3) {
    transform: translateY(-8px) rotate(-45deg);
  }

  /* อนิเมชั่นเด้งไล่ระดับของเมนูใน Sidebar */
  .sidebar-item {
    opacity: 0;
    transform: translateX(-20px);
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }
  /* คลาสโชว์ที่จะถูกยัดเข้ามาทาง JS */
  .sidebar-item.item-show {
    opacity: 1;
    transform: translateX(0);
  }

  .sidebar-item:nth-child(1) {
    transition-delay: 0.1s;
  }
  .sidebar-item:nth-child(2) {
    transition-delay: 0.15s;
  }
  .sidebar-item:nth-child(3) {
    transition-delay: 0.2s;
  }
  .sidebar-item:nth-child(4) {
    transition-delay: 0.25s;
  }
  .sidebar-item:nth-child(5) {
    transition-delay: 0.3s;
  }
  .sidebar-item:nth-child(6) {
    transition-delay: 0.35s;
  }
  header {
    font-family: kanit, sans-serif !important;
  }
</style>

<script>
  // 🌟 ใช้ JS สลับคลาส Tailwind โดยตรง!
  const hamburgerBtn = document.getElementById("hamburger-btn");
  const overlay = document.getElementById("mobile-overlay");
  const sidebar = document.getElementById("mobile-sidebar");
  const sidebarItems = document.querySelectorAll(".sidebar-item");

  const toggleSidebar = () => {
    // 1. สลับหน้าตาปุ่ม Hamburger
    hamburgerBtn?.classList.toggle("active");

    // 2. สลับ Tailwind Class ให้ Sidebar สไลด์ออกมา
    sidebar?.classList.toggle("-translate-x-full");
    sidebar?.classList.toggle("translate-x-0");

    // 3. เปิด/ปิด แผ่นฟิล์มดำ
    overlay?.classList.toggle("opacity-0");
    overlay?.classList.toggle("opacity-100");
    overlay?.classList.toggle("pointer-events-none");
    overlay?.classList.toggle("pointer-events-auto");

    // 4. ล็อกจอ & ใส่คลาสให้ตัวหนังสือเด้ง
    if (sidebar?.classList.contains("translate-x-0")) {
      document.body.style.overflow = "hidden";
      sidebarItems.forEach((item) => item.classList.add("item-show"));
    } else {
      document.body.style.overflow = "";
      sidebarItems.forEach((item) => item.classList.remove("item-show"));
    }
  };

  hamburgerBtn?.addEventListener("click", toggleSidebar);
  overlay?.addEventListener("click", toggleSidebar);

  // ปิดเมนูเวลากดลิงก์
  sidebarItems.forEach((item) => {
    item.addEventListener("click", () => {
      if (sidebar?.classList.contains("translate-x-0")) {
        toggleSidebar();
      }
    });
  });
</script>
