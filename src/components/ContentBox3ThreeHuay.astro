---
const { titleleft, titlecenter, titleright } = Astro.props;
---

<section class="mx-auto mb-8 w-full max-w-7xl px-4 font-sans">
  <div
    class="group relative w-full overflow-hidden rounded-3xl border border-red-600/30 bg-[#2a0404]/80 p-8 text-white/90 shadow-[0_10px_30px_rgba(220,38,38,0.3)] backdrop-blur-xl transition-all duration-500 hover:border-yellow-500/40 hover:shadow-[0_15px_40px_rgba(251,191,36,0.2)] md:p-10">
    <div class="relative z-10 grid grid-cols-1 gap-8 md:grid-cols-3">
      <article class="flex flex-col gap-3 border-b border-red-800/50 pb-8 md:border-r md:border-b-0 md:pr-8 md:pb-0">
        {
          titleleft && (
            <h3 class="flex items-center gap-2 text-lg font-black tracking-widest text-yellow-400 uppercase">
              <div class="h-2 w-2 rounded-full bg-yellow-400 shadow-[0_0_8px_rgba(251,191,36,1)]" /> {titleleft}
            </h3>
          )
        }
        <div class="text-sm leading-relaxed text-red-200/70"><slot name="left" /></div>
      </article>

      <article class="flex flex-col gap-3 border-b border-red-800/50 pb-8 md:border-r md:border-b-0 md:px-4 md:pb-0">
        {
          titlecenter && (
            <h3 class="flex items-center gap-2 text-lg font-black tracking-widest text-yellow-400 uppercase">
              <div class="h-2 w-2 rounded-full bg-yellow-400 shadow-[0_0_8px_rgba(251,191,36,1)]" /> {titlecenter}
            </h3>
          )
        }
        <div class="text-sm leading-relaxed text-red-200/70"><slot name="center" /></div>
      </article>

      <article class="flex flex-col gap-3 md:pl-8">
        {
          titleright && (
            <h3 class="flex items-center gap-2 text-lg font-black tracking-widest text-yellow-400 uppercase">
              <div class="h-2 w-2 rounded-full bg-yellow-400 shadow-[0_0_8px_rgba(251,191,36,1)]" /> {titleright}
            </h3>
          )
        }
        <div class="text-sm leading-relaxed text-red-200/70"><slot name="right" /></div>
      </article>
    </div>
  </div>
</section>
<style>
  .font-sans {
    font-family: "Kanit", sans-serif;
  }
</style>
